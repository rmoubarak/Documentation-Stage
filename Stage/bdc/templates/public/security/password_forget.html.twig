{% extends 'base_card.html.twig' %}

{% block title %}Mot de passe oublié{% endblock %}

{% block importmap %}
    {{ importmap(['app', 'security']) }}
{% endblock %}

{% block container %}
    <div class="row mb-5">
        <div class="offset-md-3 col-md-6 offset-lg-4 col-lg-4 mb-3">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title"><div class="badge bg-label-danger rounded"><i class='bx bx-key fs-3 p-1'></i></div>
                        Mot de passe oublié</h4>

                    <p>Veuillez saisir votre adresse email. Vous recevrez un message indiquant la procédure à suivre.</p>

                    <form id="frm_password" name="frm_password" action="{{ path('security_app_password_forget') }}" method="post" role="form">
                        <div class="input-group mt-2">
                            <span class="input-group-text"><i class="fa-solid fa-at fa-lg"></i></span>
                            <input type="email" name="email" id="email" value="" class="form-control" required="required" />
                        </div>

                        <div id="captcha" data-label="false" class="mt-2"></div>

                        <div class="mt-2 text-center">
                            <button type="submit" class="btn btn-success"><i class="fa-solid fa-check"></i> Valider</button>
                            <a href="{{ path('security_login') }}" role="button" class="btn btn-outline-secondary"><i class="fa-solid fa-sign-out-alt"></i> Annuler</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
